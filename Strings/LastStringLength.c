#include<stdio.h>

int lengthOfLastWord(char* s) {
    
}

int main() {

    char str[100], *s;
    int length=0,i,count;

    printf("Enter the string: ");
    scanf("%s",str);

    s = str;
   
    lengthOfLastWord( s );

    return 0;
}