#include<stdio.h>

int main() {

    char str[100], *s;
    int length=0,i;

    printf("Enter the string: ");
    scanf("%s",str);

    s = str;

    // for( i=0; *s[i]; i++ )
    //     length++;

    printf("%s", s);





    return 0;
}