#include<stdio.h>

void Prime( int n ){
    int i , j;
    int arr[n+1];
    for( i = 0; i <= n;i++ ){
        arr[i] = 1;
        printf("%d\t",arr[i]);

        for (  )
    }

}

int main () {

    int num;
    printf("Enter a range:");
    scanf("%d",&num);
    Prime(num);  

    return 0;
}